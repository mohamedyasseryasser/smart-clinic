@model smart_clinic.viewmodels.Appoinment.ResponseAppoimentVM;

@{
    ViewData["Title"] = "Start Visit";
}

<div class="container mt-5">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <h2>Start Visit</h2>

            <div class="card mb-3">
                <div class="card-body">
                    <p><strong>Patient:</strong> @Model.Patient?.patientname</p>
                    <p><strong>Phone Number:</strong> @Model.PhoneNumber</p>
                    <p><strong>Appointment Date:</strong> @Model.Appoinmentdate.ToString("yyyy-MM-dd")</p>
                </div>
            </div>
            <form method="post" asp-action="createvisit" asp-controller="Visit">
                <input type="hidden" name="id" value="@Model.appoimentid" />

                <div class="form-group mb-3">
                    <label for="notes" class="form-label">Visit Notes</label>
                    <textarea name="notes" id="notes" class="form-control" rows="3"></textarea>
                </div>

                <div class="form-group mb-3">
                    <label for="diagnosis" class="form-label">Diagnosis</label>
                    <textarea name="diagnosis" id="diagnosis" class="form-control" rows="3"></textarea>
                </div>

                <button type="submit" class="btn btn-primary">Start Visit</button>
                <a href="@Url.Action("Details", new { id = Model.appoimentid })" class="btn btn-secondary">Cancel</a>
            </form>
        </div>
    </div>
</div>